{{#if post.length}}
<section>
    <h2>Posts</h2>
    <ul>
        {{#each post}}
        <li>
            <a href="/post/{{this.id}}">{{this.title}}</a>
        </li>
        {{/each}}
    </ul>
</section>